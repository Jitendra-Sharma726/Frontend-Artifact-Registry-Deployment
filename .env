# Step 1: Package the build files with Ansible
ansible-playbook package.yml

# Step 2: Initialize the local Terraform state and provider plugins
./terraform init

# Step 3: Run the deployment and push the artifact to your local cloud emulator
./terraform apply -auto-approve
